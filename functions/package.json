{
  "name": "functions",
  "description": "Cloud Functions for AG Battle Zone",
  "scripts": {
    "lint": "eslint .",
    "build": "echo 'No build step required'",
    "deploy": "firebase deploy --only functions:processTransaction"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.6.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
